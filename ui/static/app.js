(function(){
  function csrf(){return document.cookie.split('; ').find(x=>x.trim().startsWith('bb_csrf='))?.split('=')[1]||''}
  function headers(extra){return Object.assign({'X-CSRF-Token':decodeURIComponent(csrf())},extra||{})}
  async function mutate(url, options){const response=await fetch(url,Object.assign({headers:headers()},options||{}));if(!response.ok){let detail='Ошибка операции';try{const body=await response.json();detail=body.message||body.detail||detail}catch(_e){}throw new Error(detail)}return response}
  document.querySelectorAll('[data-vm-action]').forEach(function(button){button.addEventListener('click',async function(){button.disabled=true;try{await mutate(`/api/v1/vms/${button.dataset.vmId}/${button.dataset.vmAction}`,{method:'POST'});location.reload()}catch(error){alert(error.message)}finally{button.disabled=false}})});
  const create=document.getElementById('create-vm');if(create){create.addEventListener('submit',async function(event){event.preventDefault();const form=new FormData(create);const payload={name:form.get('name'),protocol:form.get('protocol'),preset_id:form.get('preset_id')||null,map_version:form.get('map_version')};try{await mutate('/api/v1/vms',{method:'POST',headers:headers({'Content-Type':'application/json'}),body:JSON.stringify(payload)});location.reload()}catch(error){alert(error.message)}})}
  const upload=document.getElementById('map-upload');if(upload){upload.addEventListener('submit',async function(event){event.preventDefault();try{await mutate('/api/v1/maps/upload',{method:'POST',headers:headers(),body:new FormData(upload)});alert('Карта опубликована');upload.reset()}catch(error){alert(error.message)}})}
  const edit=document.getElementById('edit-vm');if(edit){edit.addEventListener('submit',async function(event){event.preventDefault();const form=new FormData(edit);try{await mutate(`/api/v1/vms/${edit.dataset.vmId}`,{method:'PATCH',headers:headers({'Content-Type':'application/json'}),body:JSON.stringify({name:form.get('name'),description:form.get('description'),map_version:form.get('map_version')})});location.href='/admin/vms'}catch(error){alert(error.message)}})}
  const scan=document.querySelector('[data-resource-scan]');if(scan){scan.addEventListener('click',async function(){scan.disabled=true;try{await mutate('/api/v1/resources/scan',{method:'POST'});location.reload()}catch(error){alert(error.message)}finally{scan.disabled=false}})}
  document.querySelectorAll('[data-resource-approve]').forEach(function(button){button.addEventListener('click',async function(){button.disabled=true;try{await mutate(`/api/v1/resources/${encodeURIComponent(button.dataset.resourceApprove)}/approve`,{method:'POST'});location.reload()}catch(error){alert(error.message)}finally{button.disabled=false}})});
  const live=document.getElementById('live');if(!live||!window.WebSocket)return;const protocol=location.protocol==='https:'?'wss':'ws';const socket=new WebSocket(`${protocol}://${location.host}/ws/v1/events`);socket.onmessage=function(event){const message=JSON.parse(event.data);live.dataset.lastEvent=message.seq||'';const status=document.getElementById('live-status');if(status&&message.topic==='vm_status'){status.textContent=`Событие #${message.seq}: ${message.payload.lifecycle||''}`;}const tags=document.getElementById('live-tags');if(tags&&message.topic==='tags'){tags.textContent=JSON.stringify(message.payload.tags||message.payload);}};
})();
